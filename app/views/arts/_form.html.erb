    <div class="new-form">
<%= form_for @art, html: { multipart: true }  do |f| %>
  <% if @art.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@art.errors.count, "error") %> prohibited this art from being saved:</h2>

      <ul>
      <% @art.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
    <div class="field">
    <%= f.label :album %><br>
    <%= f.collection_select  :album_id, Album.order(:title, ),:id,:title, include_blank: false, :class => "gall-select" %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image, multiple: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

</div>
